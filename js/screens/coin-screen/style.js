import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  coinText: {
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 50,
    marginBottom: 20
  },
  coinsContainer: {
    justifyContent: 'center',
    margin: 10,
  },
  container: {
    flex: 1,
  }
});
